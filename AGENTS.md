# Agent Guidelines

本项目的关键约束按“模块块”组织，便于后期分块维护和扩展。

## 模块 0：协作规范

- 默认使用简体中文回复。
- 每次完成任务后，针对修改的文件自动创建 commit，忽略非本次的修改，每项功能都单独创建。
- 代码需按功能分块组装，便于后期维护。
- 该项目仅面向登录用户,修改代码时注意添加鉴权认证,避免引发安全问题。
- 每次修改前使用context7来获取最新规范要求。
- 所有新增API均建立在原有的/api/v1上, 但是在调用的时候默认只使用/api/v3, 可以参考 API_DOCUMENTATION.md, 并在添加后自动更新文档。
- 所有涉及前端样式的修改默认只针对fantastic主题。


